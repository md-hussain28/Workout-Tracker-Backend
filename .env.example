# Copy to .env and fill in DATABASE_PASSWORD (never commit .env)
# APP
APP_NAME="Workout Tracker API"
DEBUG=false
ENVIRONMENT=development

# API
API_V1_PREFIX=/api/v1

# Database (Aiven PostgreSQL)
DATABASE_HOST=pg-2cec4e9f-saquibworkouttracker.k.aivencloud.com
DATABASE_PORT=15207
DATABASE_USER=avnadmin
DATABASE_PASSWORD=
DATABASE_NAME=defaultdb
DATABASE_SSL_MODE=require

# Pool
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10

# Production (e.g. on Render): set to your frontend URL so CORS allows it
# CORS_ORIGINS=https://your-app.vercel.app
# ENVIRONMENT=production
