# FastAPI
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Database
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0

# Config & validation
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Auth (for later)
passlib[bcrypt]>=1.7.4

# Migrations (sync driver for Alembic)
alembic>=1.14.0
psycopg2-binary>=2.9.10

# Optional: python-dotenv (pydantic-settings reads .env by default)
# python-dotenv>=1.0.0
